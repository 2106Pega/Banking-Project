package com.company.Controllers.Endpoints.TransactionsEndpoint;

import com.company.Controllers.Endpoints.Endpoint;

public class TransactionEndpoint implements Endpoint {
    @Override
    public String[] response(String[] parameters) {
        return new String[0];
    }
}
